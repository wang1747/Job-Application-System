# 匹配准确度升级计划（自改指南）

> 目标文件：`backend/app/modules/match/services.py`
> 前置结论：当前匹配不准有 **三层原因**，只换 embedding 模型解决不了全部，必须一起改。

---

## 一、现状诊断（改之前先知道为什么不准）

### 问题 1：embedding 是假的（`services.py:148-158`）

`_get_embedding` 用 MD5 把词元哈希进 384 维桶里再归一化。这本质是**词袋模型（bag-of-words）**：

- "熟悉 Python 后端开发" 和 "精通 Python 服务端编程" 几乎没有共同词元 → 相似度极低
- "Kubernetes" 和 "K8s"、"消息队列" 和 "MQ" 被当成完全不同的词
- 中文按单字 + 二元 bigram 切，语义信息基本为零

**症状**：语义分对同义表述不敏感，对无关 JD 也可能给不低的分（撞了常见词）。

### 问题 2：语义分的取值区间会压缩分数（`services.py:184-187`）

```python
similarity = 1.0 - distance      # cosine 相似度，BGE 系模型一般在 0.5 ~ 0.9 之间
return int(round(similarity * 100))
```

真语义模型（BGE）的余弦相似度天然挤在 0.5–0.9 区间。乘 100 后，"完全不相关"的 JD 也能拿 55 分，"高度匹配"也只有 85 分 —— **分数没有区分度**，混进 60% 权重后所有结果都长一个样。

### 问题 3：混合权重是拍脑袋的（`services.py:234-238`）

```python
final_score = int(semantic_score * 0.4 + local_score * 0.6)
```

两个分量语义不同：
- 语义分衡量的是"**领域相关度**"（你是不是这个方向的人）
- 词典分衡量的是"**硬技能缺口**"（JD 要的技能你有没有）

一个简历完全不会 JD 要求的技能，但领域相近，语义分照样 70+，最终分被拉高 —— 这就是"匹配不准"最直接的体感来源。**正确结构是：语义分定基准，技能缺口做扣减上限**，而不是简单加权平均。

### 附加小问题（顺手修）

- `_semantic_score` 每次匹配都对 JD 重新算 embedding 并 upsert —— 同一个 JD 被反复匹配（排行榜场景）时浪费计算
- 旧 collection `jd_resume_matches` 里存的是假向量，换模型后**新旧向量不能混在一个 collection 里**（维度/语义空间都变了）
- `requirements.txt` 是 UTF-16 编码（带 BOM），`pip install -r` 可能解析异常，建议转存为 UTF-8

---

## 二、方案选型：embedding 用什么

| 方案 | 质量 | 成本 | 依赖 | 建议 |
|------|------|------|------|------|
| A. fastembed + bge-small-zh-v1.5（本地 ONNX） | 好 | 0 | 只加 `fastembed`，不需要 torch | **首选**，CPU 上毫秒级 |
| B. SiliconFlow API 调 bge-m3 | 最好 | 约 ¥0.0005/千 token，几乎免费 | 需另配一个 embedding key | 语料库大了再升级 |
| C. chromadb 自带默认模型（MiniLM） | 英文尚可、中文差 | 0 | 无 | 不推荐，中文场景吃亏 |

选 A 的理由：你的 requirements 里已经有 `onnxruntime`、`tokenizers`、`huggingface-hub`（chromadb 带进来的），fastembed 和它们同栈，第一次运行自动下载约 100MB 模型文件，之后纯本地推理。**用户 BYOK 只覆盖 chat 模型，embedding 不应该强制用户再买一份 key** —— 本地跑是产品上更顺的选择。

---

## 三、动手步骤（按顺序，每步可独立验证）

### Step 0：先建评估集，否则你不知道改没改好（半天）

在 `backend/` 下新建 `eval/matching_eval.py` 和 `eval/cases.json`：

- `cases.json`：准备 **15~20 对** JD/简历，覆盖三种情况——高度匹配（预期 80+）、领域相关但缺技能（预期 50-70）、完全跨行（预期 <40）。可以拿你自己 + 同学的真实简历编
- 脚本逻辑：逐对调用 `calculate_match`，输出每对的分数 vs 预期分数、平均绝对误差（MAE）、排序是否正确（高分对应高预期）

**验收标准（后面所有步骤共用）**：改完后 MAE ≤ 15，且三类样本的分数区间不重叠。

### Step 1：新建统一 embedding 入口（1 小时）

新建 `backend/app/core/embeddings.py`：

```python
"""统一 embedding 入口：本地 fastembed 优先，失败降级到哈希词袋"""
import hashlib
import math
import re
import threading
from typing import List, Optional

_lock = threading.Lock()
_model = None
_DIM = 512  # bge-small-zh-v1.5 输出维度

def _get_model():
    global _model
    if _model is None:
        with _lock:
            if _model is None:
                from fastembed import TextEmbedding
                _model = TextEmbedding("BAAI/bge-small-zh-v1.5")
    return _model

def get_dim() -> int:
    return _DIM

def is_semantic_available() -> bool:
    try:
        _get_model()
        return True
    except Exception:
        return False

def embed_text(text: str) -> Optional[List[float]]:
    """真语义向量；模型不可用时返回 None（调用方降级）"""
    try:
        model = _get_model()
        return list(next(model.embed([text])))
    except Exception:
        return None

def embed_hash_fallback(text: str) -> List[float]:
    """原 MD5 词袋逻辑，保留为降级方案"""
    tokens = re.findall(r"[a-z0-9]+", (text or "").lower())
    han = re.findall(r"[\u4e00-\u9fff]", text or "")
    tokens.extend(han)
    tokens.extend("".join(han[i:i+2]) for i in range(len(han) - 1))
    vec = [0.0] * 384
    for token in tokens[:2000]:
        digest = hashlib.md5(token.encode("utf-8")).digest()
        vec[int.from_bytes(digest[:4], "big") % 384] += 1.0
    norm = math.sqrt(sum(v * v for v in vec))
    return [v / norm for v in vec] if norm else vec
```

依赖：`pip install fastembed`，并把 `fastembed==0.7.x` 加进 requirements（记得用 UTF-8 保存）。

**验证**：`python -c "from app.core.embeddings import embed_text; v = embed_text('熟悉Python后端'); print(len(v), v[:3])"` 能输出 512 维向量即通。

### Step 2：改造 `_semantic_score`（1 小时）

在 `services.py` 里做四件事：

1. **换新 collection**：`_SEMANTIC_COLLECTION = "jd_resume_matches_v2"`（维度变了，绝不能复用旧的）
2. **删掉** `_get_embedding` 和 `_tokenize_for_embedding`，改 import：
   ```python
   from app.core.embeddings import embed_text, embed_hash_fallback
   ```
3. **加 JD embedding 缓存**（模块级 dict 即可，避免重复计算）：
   ```python
   _jd_embed_cache: Dict[str, tuple] = {}  # jd_id -> (text_hash, embedding)

   def _get_jd_embedding(jd_id: str, jd_text: str):
       h = hashlib.md5(jd_text.encode()).hexdigest()
       cached = _jd_embed_cache.get(jd_id)
       if cached and cached[0] == h:
           return cached[1]
       vec = embed_text(jd_text) or embed_hash_fallback(jd_text)
       _jd_embed_cache[jd_id] = (h, vec)
       return vec
   ```
4. **改造文本构造**（这是检索质量的关键，见 Step 3），`_semantic_score` 里改为：
   ```python
   jd_vec = _get_jd_embedding(jd_id, jd_text)
   resume_vec = embed_text(resume_text) or embed_hash_fallback(resume_text)
   ```
   注意：两边必须走**同一个分支**（都真语义或都降级），否则向量空间不一致，分数无意义。可以先用 `is_semantic_available()` 判断一次，决定整轮用哪套。

### Step 3：优化送入 embedding 的文本（半小时，收益很大）

现在 `_skill_sources` 把 raw_text 一股脑拼接，简历原文动辄上千字，而 bge-small-zh 的有效窗口只有 512 token，**超长文本会被截断，后面的经历全部丢失**。

改造 `_skill_sources`，构造"结构化摘要"而不是拼接原文：

```python
def _jd_embed_text(jd) -> str:
    """JD 侧：核心信息前置，must_have 重复加权"""
    parts = [
        f"职位：{jd.position or ''} 公司：{jd.company or ''}",
        "必备技能：" + "、".join(jd.must_have or []),
        "必备技能：" + "、".join(jd.must_have or []),   # 重要内容说两遍，抵消截断
        "加分技能：" + "、".join(jd.nice_to_have or []),
        "职责要求：" + (jd.raw_text or "")[:600],
    ]
    return "\n".join(p for p in parts if p.strip())

def _resume_embed_text(resume) -> str:
    """简历侧：优先用解析后的结构化字段，raw_text 只做补充"""
    parsed = resume.parsed_json or {}
    skills = parsed.get("skills", []) if isinstance(parsed.get("skills"), list) else []
    parts = [
        "技能：" + "、".join(skills),
        (resume.raw_text or "")[:800],
    ]
    return "\n".join(p for p in parts if p.strip())
```

### Step 4：修分数合成逻辑（1 小时，本计划的核心）

替换 `calculate_match` 里第 2、3 步的逻辑：

```python
# 语义分：校准到 0-100。BGE 余弦相似度经验区间 [0.45, 0.85]
# 线性映射：sim=0.45 → 0 分，sim=0.85 → 100 分
raw_sim = _semantic_score(jd.id, jd_embed_text, resume_embed_text)  # 此处返回 0-100 相似度
SEMANTIC_FLOOR, SEMANTIC_CEIL = 45.0, 85.0
if raw_sim > 0:
    semantic_pct = max(0.0, min(1.0, (raw_sim - SEMANTIC_FLOOR) / (SEMANTIC_CEIL - SEMANTIC_FLOOR)))

    # 领域基准分：语义分决定"你是不是这个方向的人"（40-75 区间）
    domain_score = 40 + semantic_pct * 35

    # 硬缺口扣减：JD 必备技能每缺一项扣 8 分，加分项缺一项扣 2 分
    penalty = min(35, len(missing) * 8 + len(partial) * 2)

    final_score = int(round(max(0, domain_score - penalty)))
else:
    final_score = local_score  # 语义不可用时退回现有词典方案
```

为什么这样设计（面试时也能讲）：
- **语义分不再直接当分数用**，只负责定位"领域基准"（40–75），因为余弦相似度无法体现"缺了哪些硬技能"
- **硬缺口由结构化数据负责**，缺口越多扣越狠，封顶 35，保证不出现"完全跨行还高分"
- `SEMANTIC_FLOOR/CEIL` 不是拍脑袋——上线后用评估集微调这两个常数即可，属于**可校准参数**而不是魔法数

### Step 5：回归验证 + 收尾（半天）

1. 跑 Step 0 的评估脚本，对比改前/改后 MAE 和三档分数分布
2. 手工构造两个 case 确认：同义改写（"精通 K8s 容器编排" vs "熟悉 Kubernetes 集群管理"）分数差应 < 10；跨行（前端简历 vs 算法 JD）分数应 < 45
3. 删除 ChromaDB 旧 collection 数据（`jd_resume_matches`），或留着不管（已不被引用）
4. 前端 `MatchAnalysis.tsx` 无需改动——接口返回结构没变

---

## 四、总工时与顺序

| 步骤 | 内容 | 工时 |
|------|------|------|
| 0 | 评估集 | 0.5 天 |
| 1 | embeddings.py | 1 小时 |
| 2 | _semantic_score 改造 + 缓存 | 1 小时 |
| 3 | 文本构造优化 | 0.5 小时 |
| 4 | 分数合成逻辑 | 1 小时 |
| 5 | 回归验证 | 0.5 天 |

**合计约 2.5 天**。建议严格按 0→5 顺序：没有评估集就动手，你无法证明"变准了"。

## 五、后续演进（不在本计划内）

- 语料库 RAG（corpus 表 + 检索增强，见 `deploy/PLAN.md` Phase 2/3）—— 依赖本次的 `embeddings.py`，做完这次升级等于铺好了路
- embedding 升级 bge-m3 API 版：`embeddings.py` 接口不变，只换内部实现
- 评估集接入 pytest，变成常驻回归测试
